import XCTest
@testable import FRLayeredNavigationController

final class FRLayeredNavigationControllerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FRLayeredNavigationController().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
